{
  "name": "vgs-with-stripe",
  "version": "1.0.0",
  "description": "Demonstration of migrating from Stripe hosted checkout to VGS Collect with network token provisioning",
  "main": "server/index.js",
  "scripts": {
    "dev": " NODE_EXTRA_CA_CERTS=$(pwd)/outbound-route-sandbox.pem concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && NODE_EXTRA_CA_CERTS=$(pwd)/outbound-route-sandbox.pem npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["stripe", "vgs", "migration", "payment", "security"],
  "author": "Travis Mottershead",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
